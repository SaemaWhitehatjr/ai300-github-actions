name: Hello AI-300

on:
  push

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Show Repository Files
        run: ls -la

      - name: Print Message
        run: echo "Hello AI-300"
